# Tags

标签用于连接不同目录里的内容。建议从少量稳定标签开始，后续再扩展。

## 常用标签

- `daily`
- `project`
- `reference`
- `code`
- `idea`
- `todo`
- `decision`
- `review`

## 命名建议

- 使用英文小写。
- 多词标签使用连字符，例如 `machine-learning`。
- 避免创建含义过近的标签，例如同时使用 `book`、`books`、`reading`。
